부동산_매물_추천_챗봇/
│
├── app.py              # 메인 실행 파일
├── chatbot.py          # 챗봇 메인 클래스
├── config.py           # 환경 설정 및 초기화
├── database.py         # 데이터베이스 접근 클래스
├── llm_processor.py    # LLM 처리 클래스
├── models.py           # 사용자 상태 및 데이터 모델 클래스
├── recommender.py      # 추천 엔진 클래스
├── utils.py            # 유틸리티 함수 (거리 계산 등)
└── .env                # 환경 변수 파일 (API 키 등)
