absl-py==2.1.0
# aiobotocore (로컬 경로 제거됨)
# aiohappyeyeballs (로컬 경로 제거됨)
# aiohttp (로컬 경로 제거됨)
# aioitertools (로컬 경로 제거됨)
aiolimiter==1.2.1
# aiosignal (로컬 경로 제거됨)
# alabaster (로컬 경로 제거됨)
albucore==0.0.23
albumentations==2.0.5
alembic==1.14.1
# altair (로컬 경로 제거됨)
# anaconda-anon-usage (로컬 경로 제거됨)
# anaconda-catalogs (로컬 경로 제거됨)
# anaconda-client (로컬 경로 제거됨)
# anaconda-cloud-auth (로컬 경로 제거됨)
# anaconda-navigator (로컬 경로 제거됨)
# anaconda-project (로컬 경로 제거됨)
# annotated-types (로컬 경로 제거됨)
# anyio (로컬 경로 제거됨)
appdirs==1.4.4
arabic-reshaper==3.0.0
# archspec (로컬 경로 제거됨)
# argon2-cffi (로컬 경로 제거됨)
# argon2-cffi-bindings (로컬 경로 제거됨)
# arrow (로컬 경로 제거됨)
astor==0.8.1
# astroid (로컬 경로 제거됨)
# astropy (로컬 경로 제거됨)
# astropy-iers-data (로컬 경로 제거됨)
# asttokens (로컬 경로 제거됨)
astunparse==1.6.3
# async-lru (로컬 경로 제거됨)
atomicwrites==1.4.0
attrs==25.1.0
# Automat (로컬 경로 제거됨)
# autopep8 (로컬 경로 제거됨)
# Babel (로컬 경로 제거됨)
# bcrypt (로컬 경로 제거됨)
# beautifulsoup4 (로컬 경로 제거됨)
bidict==0.23.1
# binaryornot (로컬 경로 제거됨)
# black (로컬 경로 제거됨)
# bleach (로컬 경로 제거됨)
# blinker (로컬 경로 제거됨)
# bokeh (로컬 경로 제거됨)
# boltons (로컬 경로 제거됨)
# botocore (로컬 경로 제거됨)
# Bottleneck (로컬 경로 제거됨)
branca==0.8.1
# Brotli (로컬 경로 제거됨)
# cachetools (로컬 경로 제거됨)
# certifi (로컬 경로 제거됨)
# cffi (로컬 경로 제거됨)
# chardet (로컬 경로 제거됨)
# charset-normalizer (로컬 경로 제거됨)
click==8.1.8
# cloudpickle (로컬 경로 제거됨)
# colorama (로컬 경로 제거됨)
# colorcet (로컬 경로 제거됨)
# comm (로컬 경로 제거됨)
# conda (로컬 경로 제거됨)
# conda-build (로컬 경로 제거됨)
# conda-content-trust (로컬 경로 제거됨)
# conda-libmamba-solver (로컬 경로 제거됨)
# conda-pack (로컬 경로 제거됨)
# conda-package-handling (로컬 경로 제거됨)
# conda-repo-cli (로컬 경로 제거됨)
# conda-token (로컬 경로 제거됨)
# conda_index (로컬 경로 제거됨)
# conda_package_streaming (로컬 경로 제거됨)
# constantly (로컬 경로 제거됨)
# contourpy (로컬 경로 제거됨)
# cookiecutter (로컬 경로 제거됨)
# cryptography (로컬 경로 제거됨)
# cssselect (로컬 경로 제거됨)
# cycler (로컬 경로 제거됨)
Cython==3.0.12
# cytoolz (로컬 경로 제거됨)
# dask (로컬 경로 제거됨)
# dask-expr (로컬 경로 제거됨)
dataclasses-json==0.6.7
# datashader (로컬 경로 제거됨)
# debugpy (로컬 경로 제거됨)
# decorator (로컬 경로 제거됨)
# defusedxml (로컬 경로 제거됨)
# diff-match-patch (로컬 경로 제거됨)
diffimg==0.2.3
# dill (로컬 경로 제거됨)
# distributed (로컬 경로 제거됨)
# distro (로컬 경로 제거됨)
dnspython==2.7.0
# docstring-to-markdown (로컬 경로 제거됨)
# docutils (로컬 경로 제거됨)
duckduckgo_search==7.5.4
easyocr==1.7.2
email_validator==2.2.0
# et-xmlfile (로컬 경로 제거됨)
eventlet==0.39.1
# executing (로컬 경로 제거됨)
faiss-cpu==1.10.0
fake-useragent==2.0.3
# fastjsonschema (로컬 경로 제거됨)
# filelock (로컬 경로 제거됨)
fire==0.7.0
# flake8 (로컬 경로 제거됨)
# Flask (로컬 경로 제거됨)
flask-cors==5.0.1
Flask-Login==0.6.3
Flask-Mail==0.10.0
Flask-Migrate==4.1.0
Flask-SocketIO==5.5.1
Flask-SQLAlchemy==3.1.1
Flask-WTF==1.2.2
flatbuffers==25.2.10
folium==0.19.4
# fonttools (로컬 경로 제거됨)
fqdn==1.5.1
# frozendict (로컬 경로 제거됨)
# frozenlist (로컬 경로 제거됨)
# fsspec (로컬 경로 제거됨)
gast==0.6.0
# gensim (로컬 경로 제거됨)
geopandas==1.0.1
# gitdb (로컬 경로 제거됨)
# GitPython (로컬 경로 제거됨)
google-pasta==0.2.0
googlemaps==4.10.0
# greenlet (로컬 경로 제거됨)
grpcio==1.71.0
gunicorn==23.0.0
# h11 (로컬 경로 제거됨)
# h5py (로컬 경로 제거됨)
# HeapDict (로컬 경로 제거됨)
# holoviews (로컬 경로 제거됨)
# httpcore (로컬 경로 제거됨)
httpx==0.27.2
httpx-sse==0.4.0
huggingface-hub==0.30.2
# hvplot (로컬 경로 제거됨)
# hyperlink (로컬 경로 제거됨)
# idna (로컬 경로 제거됨)
# imagecodecs (로컬 경로 제거됨)
# imageio (로컬 경로 제거됨)
# imagesize (로컬 경로 제거됨)
# imbalanced-learn (로컬 경로 제거됨)
# importlib-metadata (로컬 경로 제거됨)
# incremental (로컬 경로 제거됨)
# inflection (로컬 경로 제거됨)
# iniconfig (로컬 경로 제거됨)
# intake (로컬 경로 제거됨)
# intervaltree (로컬 경로 제거됨)
# ipykernel (로컬 경로 제거됨)
# ipython (로컬 경로 제거됨)
# ipython-genutils (로컬 경로 제거됨)
# ipywidgets (로컬 경로 제거됨)
isoduration==20.11.0
# isort (로컬 경로 제거됨)
# itemadapter (로컬 경로 제거됨)
# itemloaders (로컬 경로 제거됨)
# itsdangerous (로컬 경로 제거됨)
# jaraco.classes (로컬 경로 제거됨)
# jedi (로컬 경로 제거됨)
# jellyfish (로컬 경로 제거됨)
# Jinja2 (로컬 경로 제거됨)
jiter==0.9.0
# jmespath (로컬 경로 제거됨)
# joblib (로컬 경로 제거됨)
jpype1==1.5.2
# json5 (로컬 경로 제거됨)
# jsonpatch (로컬 경로 제거됨)
jsonpointer==2.1
# jsonschema (로컬 경로 제거됨)
# jsonschema-specifications (로컬 경로 제거됨)
# jupyter (로컬 경로 제거됨)
# jupyter-console (로컬 경로 제거됨)
# jupyter-events (로컬 경로 제거됨)
# jupyter-lsp (로컬 경로 제거됨)
# jupyter_client (로컬 경로 제거됨)
# jupyter_core (로컬 경로 제거됨)
# jupyter_server (로컬 경로 제거됨)
# jupyter_server_terminals (로컬 경로 제거됨)
# jupyterlab (로컬 경로 제거됨)
# jupyterlab-pygments (로컬 경로 제거됨)
# jupyterlab-widgets (로컬 경로 제거됨)
# jupyterlab_server (로컬 경로 제거됨)
keras==3.9.0
# keyring (로컬 경로 제거됨)
# kiwisolver (로컬 경로 제거됨)
konlpy==0.6.0
langchain==0.3.21
langchain-community==0.3.20
langchain-core==0.3.48
langchain-experimental==0.3.4
langchain-faiss==0.1.1
langchain-openai==0.3.10
langchain-text-splitters==0.3.7
langsmith==0.3.18
# lazy-object-proxy (로컬 경로 제거됨)
# lazy_loader (로컬 경로 제거됨)
# lckr_jupyterlab_variableinspector (로컬 경로 제거됨)
# libarchive-c (로컬 경로 제거됨)
libclang==18.1.1
# libmambapy (로컬 경로 제거됨)
# linkify-it-py (로컬 경로 제거됨)
# llvmlite (로컬 경로 제거됨)
# lmdb (로컬 경로 제거됨)
# locket (로컬 경로 제거됨)
lxml==5.3.1
# lz4 (로컬 경로 제거됨)
Mako==1.3.9
# Markdown (로컬 경로 제거됨)
# markdown-it-py (로컬 경로 제거됨)
# MarkupSafe (로컬 경로 제거됨)
marshmallow==3.26.1
matplotlib==3.9.2
# matplotlib-inline (로컬 경로 제거됨)
# mccabe (로컬 경로 제거됨)
# mdit-py-plugins (로컬 경로 제거됨)
# mdurl (로컬 경로 제거됨)
mecab-python==1.0.0
mecab-python3==1.0.10
# menuinst (로컬 경로 제거됨)
# mistune (로컬 경로 제거됨)
# mkl-service==2.4.0
# mkl_fft (로컬 경로 제거됨)
# mkl_random (로컬 경로 제거됨)
ml_dtypes==0.5.1
# more-itertools (로컬 경로 제거됨)
# mpmath (로컬 경로 제거됨)
# msgpack (로컬 경로 제거됨)
# multidict (로컬 경로 제거됨)
# multipledispatch (로컬 경로 제거됨)
# mypy (로컬 경로 제거됨)
# mypy-extensions (로컬 경로 제거됨)
mysql-connector-python==9.2.0
mysqlclient==2.2.7
namex==0.0.8
natsort==8.4.0
# navigator-updater (로컬 경로 제거됨)
# nbclient (로컬 경로 제거됨)
# nbconvert (로컬 경로 제거됨)
# nbformat (로컬 경로 제거됨)
# nest-asyncio (로컬 경로 제거됨)
# networkx (로컬 경로 제거됨)
ninja==1.11.1.4
# nltk (로컬 경로 제거됨)
# notebook (로컬 경로 제거됨)
# notebook_shim (로컬 경로 제거됨)
# numba (로컬 경로 제거됨)
# numexpr (로컬 경로 제거됨)
# numpy (로컬 경로 제거됨)
# numpydoc (로컬 경로 제거됨)
openai==1.68.2
openai-whisper==20240930
opencv-contrib-python==4.11.0.86
opencv-python==4.11.0.86
opencv-python-headless==4.11.0.86
# openpyxl (로컬 경로 제거됨)
opt-einsum==3.3.0
optree==0.14.1
orjson==3.10.16
outcome==1.3.0.post0
# overrides (로컬 경로 제거됨)
# packaging (로컬 경로 제거됨)
paddleocr==2.10.0
# protobuf==3.20.2 (충돌로 제거됨)
# transformers==4.51.3 (protobuf 의존성 충돌로 제거됨)
# pandas (로컬 경로 제거됨)
# pandocfilters (로컬 경로 제거됨)
# panel (로컬 경로 제거됨)
# param (로컬 경로 제거됨)
# paramiko (로컬 경로 제거됨)
# parsel (로컬 경로 제거됨)
# parso (로컬 경로 제거됨)
# partd (로컬 경로 제거됨)
# pathspec (로컬 경로 제거됨)
# patsy (로컬 경로 제거됨)
# pexpect (로컬 경로 제거됨)
# pickleshare (로컬 경로 제거됨)
# pillow (로컬 경로 제거됨)
pinecone==6.0.2
pinecone-plugin-interface==0.0.7
# pkce (로컬 경로 제거됨)
# pkginfo (로컬 경로 제거됨)
# platformdirs (로컬 경로 제거됨)
# plotly (로컬 경로 제거됨)
# pluggy (로컬 경로 제거됨)
# ply (로컬 경로 제거됨)
primp==0.14.0
# prometheus-client (로컬 경로 제거됨)
# prompt-toolkit (로컬 경로 제거됨)
# Protego (로컬 경로 제거됨)
# protobuf==3.20.2  # 충돌로 인해 주석 처리됨
# psutil (로컬 경로 제거됨)
# ptyprocess (로컬 경로 제거됨)
# pure-eval (로컬 경로 제거됨)
# py-cpuinfo (로컬 경로 제거됨)
# pyarrow (로컬 경로 제거됨)
# pyasn1 (로컬 경로 제거됨)
pyasn1-modules==0.2.8
pyclipper==1.3.0.post6
# pycodestyle (로컬 경로 제거됨)
# pycosat (로컬 경로 제거됨)
# pycparser (로컬 경로 제거됨)
# pyct (로컬 경로 제거됨)
# pycurl (로컬 경로 제거됨)
pydantic==2.11.1
pydantic-settings==2.8.1
pydantic_core==2.33.0
# pydeck (로컬 경로 제거됨)
# PyDispatcher (로컬 경로 제거됨)
# pydocstyle (로컬 경로 제거됨)
# pyerfa (로컬 경로 제거됨)
# pyflakes (로컬 경로 제거됨)
# Pygments (로컬 경로 제거됨)
# PyJWT (로컬 경로 제거됨)
# pylint (로컬 경로 제거됨)
# pylint-venv (로컬 경로 제거됨)
pyls-spyder==0.4.0
PyMySQL==1.1.1
# PyNaCl (로컬 경로 제거됨)
# pyodbc (로컬 경로 제거됨)
pyogrio==0.10.0
# pyOpenSSL (로컬 경로 제거됨)
# pyparsing (로컬 경로 제거됨)
PyPDF2==3.0.1
pyproj==3.7.1
PyQt5==5.15.10
# PyQt5-sip (로컬 경로 제거됨)
PyQtWebEngine==5.15.6
# PySocks (로컬 경로 제거됨)
pytesseract==0.3.13
# pytest (로컬 경로 제거됨)
python-bidi==0.4.2
# python-dateutil (로컬 경로 제거됨)
python-docx==1.1.2
# python-dotenv (로컬 경로 제거됨)
python-engineio==4.11.2
# python-json-logger (로컬 경로 제거됨)
# python-lsp-black (로컬 경로 제거됨)
# python-lsp-jsonrpc (로컬 경로 제거됨)
# python-lsp-server (로컬 경로 제거됨)
# python-slugify (로컬 경로 제거됨)
python-socketio==5.12.1
# pytoolconfig (로컬 경로 제거됨)
# pytz (로컬 경로 제거됨)
# pyviz_comms (로컬 경로 제거됨)
# PyWavelets (로컬 경로 제거됨)
# pywin32==305.1
# pywin32-ctypes (로컬 경로 제거됨)
# pywinpty (로컬 경로 제거됨)
# PyYAML (로컬 경로 제거됨)
# pyzmq (로컬 경로 제거됨)
# QDarkStyle (로컬 경로 제거됨)
# qstylizer (로컬 경로 제거됨)
# QtAwesome (로컬 경로 제거됨)
# qtconsole (로컬 경로 제거됨)
# QtPy (로컬 경로 제거됨)
# queuelib (로컬 경로 제거됨)
RapidFuzz==3.12.2
ratelimiter==1.2.0.post0
# referencing (로컬 경로 제거됨)
# regex (로컬 경로 제거됨)
# requests (로컬 경로 제거됨)
# requests-file (로컬 경로 제거됨)
# requests-toolbelt (로컬 경로 제거됨)
# rfc3339-validator (로컬 경로 제거됨)
# rfc3986-validator (로컬 경로 제거됨)
# rich (로컬 경로 제거됨)
# rope (로컬 경로 제거됨)
# rpds-py (로컬 경로 제거됨)
# Rtree (로컬 경로 제거됨)
# ruamel-yaml-conda (로컬 경로 제거됨)
# ruamel.yaml (로컬 경로 제거됨)
# ruamel.yaml.clib (로컬 경로 제거됨)
# s3fs (로컬 경로 제거됨)
safetensors==0.5.3
schedule==1.2.2
# scikit-image (로컬 경로 제거됨)
# scikit-learn (로컬 경로 제거됨)
# scipy (로컬 경로 제거됨)
# Scrapy (로컬 경로 제거됨)
# seaborn (로컬 경로 제거됨)
selenium==4.28.1
# semver (로컬 경로 제거됨)
# Send2Trash (로컬 경로 제거됨)
sentence-transformers==4.1.0
# service-identity (로컬 경로 제거됨)
setuptools==75.1.0
shapely==2.0.7
simple-websocket==1.1.0
simsimd==6.2.1
# sip (로컬 경로 제거됨)
# six (로컬 경로 제거됨)
# smart-open (로컬 경로 제거됨)
# smmap (로컬 경로 제거됨)
# sniffio (로컬 경로 제거됨)
# snowballstemmer (로컬 경로 제거됨)
# sortedcontainers (로컬 경로 제거됨)
# soupsieve (로컬 경로 제거됨)
# Sphinx (로컬 경로 제거됨)
# sphinxcontrib-applehelp (로컬 경로 제거됨)
# sphinxcontrib-devhelp (로컬 경로 제거됨)
# sphinxcontrib-htmlhelp (로컬 경로 제거됨)
# sphinxcontrib-jsmath (로컬 경로 제거됨)
# sphinxcontrib-qthelp (로컬 경로 제거됨)
# sphinxcontrib-serializinghtml (로컬 경로 제거됨)
# spyder (로컬 경로 제거됨)
# spyder-kernels (로컬 경로 제거됨)
# SQLAlchemy (로컬 경로 제거됨)
# stack-data (로컬 경로 제거됨)
# statsmodels (로컬 경로 제거됨)
# streamlit (로컬 경로 제거됨)
streamlit-chat==0.1.1
stringzilla==3.12.3
sympy==1.13.1
# tables (로컬 경로 제거됨)
# tabulate (로컬 경로 제거됨)
# tblib (로컬 경로 제거됨)
# tenacity (로컬 경로 제거됨)
tensorboard==2.19.0
tensorboard-data-server==0.7.2
tensorflow==2.19.0
termcolor==2.5.0
# terminado (로컬 경로 제거됨)
# text-unidecode (로컬 경로 제거됨)
# textdistance (로컬 경로 제거됨)
# threadpoolctl (로컬 경로 제거됨)
# three-merge (로컬 경로 제거됨)
# tifffile (로컬 경로 제거됨)
tiktoken==0.9.0
# tinycss2 (로컬 경로 제거됨)
# tldextract (로컬 경로 제거됨)
tokenizers==0.21.1
# toml (로컬 경로 제거됨)
# tomli (로컬 경로 제거됨)
# tomlkit (로컬 경로 제거됨)
# toolz (로컬 경로 제거됨)
torch==2.2.0
torchvision==0.17.0
# tornado (로컬 경로 제거됨)
# tqdm (로컬 경로 제거됨)
# traitlets (로컬 경로 제거됨)
transformers==4.51.3
trio==0.28.0
trio-websocket==0.11.1
# truststore (로컬 경로 제거됨)
# Twisted (로컬 경로 제거됨)
# twisted-iocpsupport (로컬 경로 제거됨)
typing-inspect==0.9.0
typing-inspection==0.4.0
typing_extensions==4.13.0
# tzdata (로컬 경로 제거됨)
# uc-micro-py (로컬 경로 제거됨)
# ujson (로컬 경로 제거됨)
ultralytics==8.3.88
ultralytics-thop==2.0.14
# unicodedata2 (로컬 경로 제거됨)
# Unidecode (로컬 경로 제거됨)
uri-template==1.3.0
# urllib3 (로컬 경로 제거됨)
# w3lib (로컬 경로 제거됨)
# watchdog (로컬 경로 제거됨)
# wcwidth (로컬 경로 제거됨)
webcolors==24.11.1
webdriver-manager==4.0.2
# webencodings (로컬 경로 제거됨)
# websocket-client (로컬 경로 제거됨)
# Werkzeug (로컬 경로 제거됨)
# whatthepatch (로컬 경로 제거됨)
wheel==0.44.0
# widgetsnbextension (로컬 경로 제거됨)
wikipedia==1.4.0
# win-inet-pton (로컬 경로 제거됨)
# wrapt (로컬 경로 제거됨)
wsproto==1.2.0
WTForms==3.2.1
# xarray (로컬 경로 제거됨)
xgboost==2.1.4
# xlwings (로컬 경로 제거됨)
# xyzservices (로컬 경로 제거됨)
# yapf (로컬 경로 제거됨)
# yarl (로컬 경로 제거됨)
# zict (로컬 경로 제거됨)
# zipp (로컬 경로 제거됨)
# zope.interface (로컬 경로 제거됨)
# zstandard (로컬 경로 제거됨)
# transformers==4.38.2  # 최신 안정버전, protobuf 4.x와 호환